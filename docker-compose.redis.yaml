version: '3.8'
services:  
  redis:
    container_name: redis
    image: redis:6.2.13-alpine
    restart: always
    ports:
      - '6379:6379'
    networks:
      -  redis-network

networks:
  redis-network:
    name: base_network
    external: true


  